GET child trip
---
tags:
   - Parent

parameters:
   - in: path
     name: Trip
     required: true

responses:
   200:
      description: an object of the child trip info

   401:
      description: Fails to get items due to authentication error
